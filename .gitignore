# R / RStudio
.Rproj.user/
.Rhistory
.RData
.Ruserdata
*.Rproj

# rsconnect deployment records (local)
rsconnect/

# OS noise
.DS_Store
Thumbs.db

# NOTE: data/sjr_all.parquet is intentionally NOT ignored.
# It is committed so the deployed app (and Posit Connect Cloud) has SJR data.
