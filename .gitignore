# R / RStudio
.Rproj.user/
.Rhistory
.RData
.Ruserdata
*.Rproj

# rsconnect deployment records (local)
rsconnect/

# OS noise
.DS_Store
Thumbs.db

# NOTE: data/sjr_all.parquet (or data/sjr_all.csv.gz) is intentionally NOT ignored.
# It is committed so the deployed app (and Posit Connect Cloud) has SJR data.

# Python
__pycache__/
*.pyc
